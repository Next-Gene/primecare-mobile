import 'package:flutter/material.dart';

class Buy_page extends StatelessWidget {
  const Buy_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("buy page"),
    );
  }
}
